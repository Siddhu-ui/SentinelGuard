# Netlify build config (used when the site is connected to the Git repo).
# The API base is injected at build time from the VITE_API_URL environment
# variable (set it in Site configuration -> Environment variables).
[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
